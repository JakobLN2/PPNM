#include"vector.h"

// template<typename T>
// vector<T> operator+(const vector<T>& a, const vector<T>& b) {
//     vector<T> res(a.size());
//     for(int i = 0 ; i < a.size() ; ++i) res[i] = a[i] + b[i];
//     return res;
// }