#include"vector.h"
#include<iostream>
#include<string>


int main() {
    vector<double> a(3);
    std::cout << a.size << std::endl;
    // a[0] = 1; a[1] = 3; a[2] = 9;
    // std::cout << a[0] << " " << a[1] << " " << a[2] << std::endl;
    // vector<double> b(3);
    // std::cout << a.size << " " << b.size << std::endl;

    return 0;
}